% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01-s4_generics.R, R/03-s4_methods.R
\name{instantiate}
\alias{instantiate}
\alias{instantiate,KFold-method}
\alias{instantiate,StratifiedKFold-method}
\alias{instantiate,MonteCarlo-method}
\alias{instantiate,BalancedMonteCarlo-method}
\alias{instantiate,BalancedBootstrap-method}
\title{Instantiate a resampling strategy}
\usage{
instantiate(object, Y)

\S4method{instantiate}{KFold}(object, Y)

\S4method{instantiate}{StratifiedKFold}(object, Y)

\S4method{instantiate}{MonteCarlo}(object, Y)

\S4method{instantiate}{BalancedMonteCarlo}(object, Y)

\S4method{instantiate}{BalancedBootstrap}(object, Y)
}
\arguments{
\item{object}{A resampling strategy object (e.g. \code{MonteCarlo},
\code{BalancedMonteCarlo}, \code{KFold}, or related classes).}

\item{Y}{Outcome data used to derive resampling splits. Typically a
numeric vector or matrix with observations in rows. Some strategies
(e.g. balanced or stratified variants) use \code{Y} to preserve
class proportions or outcome distribution.}
}
\value{
An object of class \code{ResamplingInstance}. This object
stores the training indices for each resampling iteration, the
originating strategy, the number of observations, and the RNG seed
used to generate the splits.
}
\description{
Generates a concrete resampling instance from a resampling strategy.
The resulting object contains the training set indices for each
resampling split and records the random seed used during generation.
}
\details{
Instantiation converts an abstract resampling strategy into concrete
training index sets. The exact behaviour depends on the resampling strategy.

The RNG seed is stored in the resulting instance to ensure that
resampling can be reproduced if needed.
}
\examples{
n <- 100
thr <- 1.5
Y <- c(rnorm(80, thr - 3, 0.3), rnorm(20, thr + 3, 0.3))  # unbalanced outcome
cv_k  <- kfold(k = 10)
k_inst <- instantiate(cv_k,  matrix(Y, ncol = 1))
sapply(train_idc(k_inst), function(i) length(i))

}
\seealso{
Other resampling strategies: 
\code{\link{balanced_boot}()},
\code{\link{balanced_mc}()},
\code{\link{kfold}()},
\code{\link{mc}()},
\code{\link{stratified_kfold}()}
}
\concept{resampling strategies}
